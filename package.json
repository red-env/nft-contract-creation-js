{
  "name": "nft-contract-creation-js",
  "version": "1.0.0",
  "description": "Simple code in javascript to deploy an ERC721 smart contract. This example is able to deploy the smart contract on *ROPSTEIN* testnet.",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Geremia Pompei",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "^1.0.41-1",
    "dotenv": "^16.0.2"
  }
}
